<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	
	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="com.company.mypage.member.vo.MemberVo" >
		insert into member(
			userNo,
			email,
			password,
			userName,
			nickName,
			phone
		) values (
			mypage_mem.nextval,
			#{email},
			#{password},
			#{userName},
			#{nickName},
			#{phone1}||-||#{phone2}||-||#{phone3}
		)
	</insert>
	
	<!-- 이메일 증복체크 -->
	<select id="emailCheck" resultType="int">
		select count(*)
		from member
		where email = #{email}
	</select>
	
	<!-- 닉네이 증복체크 -->
	<select id="nickNameCheck" resultType="int">
		select count(*)
		from member
		where nickName = #{nickName}
	</select>
	
	<!-- 로그인 -->
	<select id="selectLogin" resultType="com.company.mypage.member.vo.MemberVo">
		select 
			email, password
		from member
			where email = #{email}
			and password = #{password}
	</select>
	
</mapper>